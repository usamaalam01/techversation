# LangChain core + all supported provider packages
# Only the package matching your LLM_PROVIDER is actually used at runtime,
# but installing all of them keeps the switch effortless.
langchain-core>=0.3.0
langchain-google-genai>=2.0.0   # LLM_PROVIDER=gemini
langchain-anthropic>=0.3.0      # LLM_PROVIDER=anthropic
langchain-groq>=0.2.0           # LLM_PROVIDER=groq
langchain-openai>=0.2.0         # LLM_PROVIDER=openai or deepseek

# Fetching & parsing
feedparser>=6.0.11
httpx>=0.27.0

# File handling
python-slugify>=8.0.4
pyyaml>=6.0.2
python-frontmatter>=1.1.0
