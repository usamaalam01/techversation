---
title: "Building with Granite 4.1 LLMs"
description: "Developers guide to Granite 4.1 large language models."
date: "2026-04-29"
author: "Usama"
tags: ["llm", "huggingface", "ai"]
aiSummary: "Granite 4.1 LLMs are a new generation of large language models. This post provides a step-by-step guide to using and building with Granite 4.1. We'll cover the basics of LLMs, setting up the Hugging Face library, and fine-tuning a Granite 4.1 model."
category: "tutorials"
draft: false
---

## Introduction to Granite 4.1 LLMs

Large Language Models (LLMs) have revolutionized the field of natural language processing. The recent release of Granite 4.1 LLMs by IBM, available on the Hugging Face platform, has generated significant interest among developers and AI practitioners. In this tutorial, we'll delve into the world of Granite 4.1 LLMs, exploring how they're built and how to work with them.

### What are LLMs?

Large Language Models are a type of artificial intelligence designed to process and understand human language. They're trained on vast amounts of text data, which enables them to generate coherent and contextually relevant text. LLMs have numerous applications, including language translation, text summarization, and chatbots.

### Setting up the Hugging Face Library

To work with Granite 4.1 LLMs, you'll need to install the Hugging Face library. You can do this by running the following command in your terminal:

```bash
pip install transformers
```

Once installed, you can import the library in your Python script:

```python
import torch
from transformers import AutoModelForCausalLM, AutoTokenizer
```

## Loading a Pre-Trained Granite 4.1 Model

Hugging Face provides pre-trained Granite 4.1 models that you can use for your projects. To load a pre-trained model, you can use the `AutoModelForCausalLM` class:

```python
model_name = "ibm/granite-4-1-base"
model = AutoModelForCausalLM.from_pretrained(model_name)
tokenizer = AutoTokenizer.from_pretrained(model_name)
```

In this example, we're loading the base version of the Granite 4.1 model. You can choose from various model sizes, including `base`, `large`, and `xl`.

## Fine-Tuning a Granite 4.1 Model

Fine-tuning a pre-trained model allows you to adapt it to your specific use case. To fine-tune a Granite 4.1 model, you'll need a dataset of text examples. For this example, we'll use a simple dataset of sentences:

```python
train_data = [
    {"text": "This is a sample sentence.", "label": 0},
    {"text": "Another sample sentence.", "label": 0},
    {"text": "A sentence with a different label.", "label": 1},
]
```

Next, you'll need to preprocess the data using the `tokenizer`:

```python
input_ids = []
attention_masks = []

for example in train_data:
    inputs = tokenizer(example["text"], return_tensors="pt", max_length=512, padding="max_length", truncation=True)
    input_ids.append(inputs["input_ids"].flatten())
    attention_masks.append(inputs["attention_mask"].flatten())
```

Now, you can create a custom dataset class to store the preprocessed data:

```python
class GraniteDataset(torch.utils.data.Dataset):
    def __init__(self, input_ids, attention_masks, labels):
        self.input_ids = input_ids
        self.attention_masks = attention_masks
        self.labels = labels

    def __getitem__(self, idx):
        return {
            "input_ids": self.input_ids[idx],
            "attention_mask": self.attention_masks[idx],
            "labels": self.labels[idx],
        }

    def __len__(self):
        return len(self.input_ids)

dataset = GraniteDataset(input_ids, attention_masks, [example["label"] for example in train_data])
```

Finally, you can fine-tune the model using the `Trainer` class from the `transformers` library:

```python
from transformers import Trainer, TrainingArguments

training_args = TrainingArguments(
    output_dir="./results",
    num_train_epochs=3,
    per_device_train_batch_size=16,
    per_device_eval_batch_size=64,
    warmup_steps=500,
    weight_decay=0.01,
    logging_dir="./logs",
)

trainer = Trainer(
    model=model,
    args=training_args,
    train_dataset=dataset,
    eval_dataset=dataset,
)

trainer.train()
```

This code fine-tunes the Granite 4.1 model on your dataset for three epochs.

## Generating Text with a Granite 4.1 Model

Once you've fine-tuned the model, you can use it to generate text. To do this, you can use the `generate` method:

```python
input_text = "This is a sample input."
inputs = tokenizer(input_text, return_tensors="pt")
output = model.generate(inputs["input_ids"], max_length=512)

print(tokenizer.decode(output[0], skip_special_tokens=True))
```

This code generates text based on the input prompt.

## Conclusion

In this tutorial, we've explored the world of Granite 4.1 LLMs, covering the basics of LLMs, setting up the Hugging Face library, loading a pre-trained model, fine-tuning a model, and generating text. With this knowledge, you can start building your own projects using Granite 4.1 LLMs. Remember to visit the [Hugging Face blog](https://huggingface.co/blog/ibm-granite/granite-4-1) for more information on Granite 4.1 LLMs.

For more information on the Hugging Face library and Granite 4.1 LLMs, you can visit the [Hugging Face documentation](https://huggingface.co/docs) and the [Granite 4.1 model page](https://huggingface.co/ibm/granite-4-1-base).